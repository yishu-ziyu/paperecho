# 采集

库是快路径。现场扔飞机也可以短预算爬公开页，失败返回空，不挡开口。爬到的东西改写过 QA 才进 `COLLECTED`。

```bash
npm run collect
# 或 COLLECT_TARGET=24 COLLECT_MAX_SCRAPE=40 npm run collect
```

只抓公开页。原文落在 gitignored 的 `corpus/`，不进游戏。改写稿过 QA 后才写入 `COLLECTED`。
