# 补纸 · 玩家验收打回：日子和回访仍是上一句原样

可以改代码。不要开 PR，不要碰 .env，不要 git commit。

## 玩家在浏览器里看见的

按人分本、信柜「再去找他」、空白一张遇新人：过了。

不过：把 Jonah.lastWritten 改成 2026-08-25 再硬刷新。26/27/28 日期对了、一天一条，但三天的 text 全是基准句原样复制。回访第一句是「后来我也就没再打开过。」= lastEcho / page，一字不差。把 page 改成「基准日」，回访第一句就变成「基准日」。说明开口在读存储，MiniMax 没写出新的一天。

第二人 Diego 的开场「十七稿我打成一包，塞进抽屉最下层。」和 Jonah 的开场完全一样。核心开口不能跨人复用同一句写死的开场。

## 必须改成怎样

1. 缺的每一天：必须真正调用现有 MiniMax（runVoice / speakDay，MINIMAX_*，不碰 .env）。收回的句子写入 days[date]。不同日期的 text 不得相同（除非模型自己回了相同的，那也必须有三次独立调用）。
2. 模型没回来：不要把上一句写进新的一天冒充补写。这一天空着或标未写，等下次打开再问模型。禁止 writeDay(prev) 当成新的一天。
3. 再见面第一句：必须再走一次 MiniMax（greet / away），站在已补的日子上新说。禁止 speakFromDays 直接把 lastEcho/page 吐出来当第一句。
4. 单测：假模型每调用一次返回不同句子；断言 catchUp 调了 3 次、三天文本不同；模型返回 null 时 days 不出现「用上一句填的新日期」。tsc 过。

## 怎样算过

我再玩：硬刷新，Jonah.lastWritten=2026-08-25。网络里三次 runVoice。三天三句新的。回访第一句不等于 lastEcho，也不等于「基准日」。Diego 开场不等于 Jonah 开场。

切片 3 已过的（分本、找旧人、遇新人、上海日历、一天一条）不许弄坏。
