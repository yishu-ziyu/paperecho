# 纸上的回声 · 交互修改记录

> 用途：逐幕记录讨论后确认要修改的交互细节。每条写到代码级：现状（文件+行为）、改成什么、实现步骤、验收标准。
> 约定：英文术语后附中文括号解释。完成一项把状态改成 ✅ 已完成并注明 commit。
> 创建：2026-08-22 ｜ 最近更新：2026-08-23（飞行回到等待巡航，无跟飞、无躲障）

---

## 第一幕（title 标题 → orbit 轨道 → mirror 镜认）

### T1. title → orbit：卡片"摊开成房间"，替换飞出动画

**现状**

- `src/game/continuum.tsx` 的 `PullCommit`（下拉提交组件）在 `onUp` 里：位移超过 `threshold`（阈值）后执行 `animate(y, threshold * 2.6 * sign, spring.launch)`——卡片向下飞出屏幕，240ms 后 `setTimeout` 调 `onCommit`。
- `src/game/GameShell.tsx` 用 `AnimatePresence mode="sync"`（同步模式，新旧场景同时挂载）+ 0.28s 透明度交叉淡入切换 phase（阶段）。
- `src/game/continuum.tsx` 的 `Craft` 组件带 `layoutId="echo-craft"`（布局形变标识），设计意图是"卡片→桌面→信纸→飞机→回信是同一个物体"，但目前 title 的 Craft 和 orbit 的 Craft 之间没有真正形变——因为卡片先飞走了。
- 后果：手把卡片拉下去 → 卡片消失 → 短暂空窗 → 新场景淡入。"手里的东西"断了，违背 motion.ts 头部注释里自己写的 Continuum rule（连续性原则："the hand never lets go of an object into a button"，手不会把东西交给一个按钮）。

**改成**

下拉标题卡 = 用手把房间"拉开"：

1. 拉动中：卡片跟随手指（现有行为保留）。
2. 松手过阈值：卡片**不飞出**。改为：
   - `PullCommit` 立即触发 `onCommit`（不等 240ms）；
   - 同时把卡片自身的 x/y 用 `spring.settle`（无回弹弹簧，stiffness 280 / damping 34）归零；
   - phase 切换后，`layoutId="echo-craft"` 接管：title 的 `rounded-3xl bg-paper px-6 py-8` 卡片形变为 orbit 的 `inset-[6%] rounded-full bg-paper/70` 圆形纸场。
3. 视觉上：手里的纸"摊大"成了房间的地面。

**实现步骤**

1. `continuum.tsx` · `PullCommit` 新增 prop：
   ```ts
   commitBehavior?: "fly" | "morph";  // 默认 "fly"，不影响其他调用方
   ```
   `onUp` 里分支：
   ```ts
   if (enabledRef.current && dist > threshold) {
     locked.current = true;
     setArmed(false);
     if (commitBehaviorRef.current === "morph") {
       // 不飞出：先切场景，让 layoutId 形变接管；自身位移归零
       commitRef.current();
       void animate(x, 0, spring.settle);
       void animate(y, 0, spring.settle);
     } else {
       const fly = threshold * 2.6 * sign;
       void animate(axis === "y" ? y : x, fly, spring.launch);
       window.setTimeout(() => commitRef.current(), 240);
     }
     return;
   }
   ```
   （`commitBehavior` 和现有 `onCommit` 一样用 ref 持有，避免 effect 重挂。）
2. `TitlePhase.tsx`：标题卡的 `PullCommit` 加 `commitBehavior="morph"`。
3. `GameShell.tsx`：确认 `AnimatePresence mode="sync"` 下两个 phase 同时挂载（现状已是），layoutId 形变才能发生。若形变时旧卡片随退出动画变淡影响观感，给退出态的 `[data-craft]` 单独设 `exit={{ opacity: 1 }}`（让形变清晰可见，不被淡出盖住）。
4. 标题卡内部文字（标题/副标题/"往下拉"）在形变中会显得突兀：包一层 `motion.div`，在 `exit` 时快速淡出文字（0.15s），只留下纸面参与形变。

**验收标准**

- [ ] 下拉松手后，卡片在原位摊大成 orbit 的圆形纸场，全程无飞出、无黑屏空窗。
- [ ] 形变时长约 0.3–0.4s，无跳帧（frame drop）。
- [ ] 信柜卡、orbit 的「你」等其他 `PullCommit` 调用保持原飞出行为（回归测试）。
- [ ] `useReducedMotion`（减弱动效偏好）下退化为点击进入，不受影响。

**风险/注意**

- MotionValue（动画数值）与 layoutId 共存：提交瞬间必须先归零 x/y 再让形变开始，否则形变起点会带着下拉残留位移，出现"跳一下"。
- orbit 的 Craft 是 `pointer-events-none absolute inset-[6%]`，和 title 卡的尺寸差异大，形变时注意 borderRadius 也要参与过渡（motion 的 layoutId 默认处理，若出现硬切，在两个 Craft 上都显式传 `transition={spring.settle}`）。

---

### T2. 拉动过程反馈：进度显影 + 过阈值的"咔哒"感

**现状**

- `PullCommit` 的提示（如"松开，进房间"）只在 `armed`（已武装，即拉过阈值）为 true 后渲染；拉到一半没有任何进度提示。
- 橡胶阻尼 `rubber()`（拉越远位移越小的阻力函数，上限 `threshold * 1.45`）让后半段位移增量变小，用户容易误判"拉不动了/到头了"。
- `onProgress`（进度回调，0→1）已存在于接口里，`OrbitPhase` 接了（驱动 `gather` 聚拢效果），`TitlePhase` 和信柜都没接。

**改成**

1. **全程进度反馈**：`PullCommit` 内部渲染一个进度提示元素（卡片下方一行字），透明度/位移由进度 t 直接驱动，不需要调用方接 `onProgress`：
   - t = 0 → 完全透明；
   - t → 1 → 渐显为完整文字；
   - 文字内容用 `hint`（如"松开，进房间"）。
2. **过阈值瞬间（armed false→true）**：
   - 文字颜色变为 `text-coral`（珊瑚红，现有 armed 样式保留）；
   - 播 `sfxCharge()`（audio.ts 里已有的蓄力音效：620Hz + 880Hz 两声短音）——**不用新增音效函数**；
   - `navigator.vibrate?.(10)`（手机轻震动 10 毫秒，可选，桌面端自动无效）。
3. 现有"armed 后才显示"的 `<span>` 删除，由上面的进度提示替代。

**实现步骤**

1. `continuum.tsx` · `PullCommit`：
   - 新增内部 state 或 MotionValue：`const progress = useMotionValue(0)`，在 `onMove` 里 `progress.set(t)`；
   - 进度提示元素：
     ```tsx
     <motion.span
       className="pointer-events-none absolute -bottom-6 left-1/2 -translate-x-1/2 whitespace-nowrap text-[0.65rem] tracking-[0.18em]"
       style={{ opacity: progress, color: armed ? "var(--color-coral)" : "var(--color-ink)" }}
     >
       {enabled ? hint : disabledHint}
     </motion.span>
     ```
     （透明度用 MotionValue 驱动，避免 setState 重渲染整个可拖动组件。）
   - armed 跳变检测：用 ref 记录上一次 armed，`onMove` 里 `if (!prevArmed && nextArmed) { sfxCharge(); navigator.vibrate?.(10); }`。disabled 状态下不播音效，只显示 `disabledHint`。
2. `TitlePhase.tsx` / 信柜：不用改（反馈内置在 PullCommit 里），只需确认 `hint` / `disabledHint` 文案合适。
3. `OrbitPhase.tsx`：「你」的 PullCommit 已有 `onProgress={setGather}`，保留；新的内置进度提示会同时出现，确认视觉上不打架（gather 聚拢的是情绪块，进度提示在 blob 下方，位置不同，OK）。

**验收标准**

- [ ] 拉 10% 就能看到提示字开始显影，拉满时完整可读。
- [ ] 过阈值瞬间有音效 + 震动（移动端），且**只在过阈值那一刻播一次**，来回拉动不重复连播。
- [ ] disabled（未满足条件，如还没有情绪靠近时拉「你」）时显示 `disabledHint`，不播音效。
- [ ] 减弱动效偏好下点击提交，流程不受影响。

**风险/注意**

- `sfxCharge` 依赖 AudioContext（音频上下文），浏览器要求用户手势后才能出声——title 的 PullCommit 本身就是手势，没问题；但要确认 `unlockAudio()` 已在首次 pointerdown 调用（目前在 `enter()` 即提交后才解锁，**过阈值音效发生在提交之前，可能无声**——把 `unlockAudio()` 挪到 PullCommit 的 `onPointerDown` 里调一次）。
- 进度提示用 MotionValue 而非 state，避免每帧重渲染 dragging 组件。

---

### T3. 信柜方向语法统一为"往下"

**现状**

- `TitlePhase.tsx` 信柜：`sign={-1}`（往上拉开信柜），`threshold={42}`。
- 全局语法是"往下 = 带走/提交"（README 玩法里 9 步全部是"往下/拖近"）；return（回信）阶段"再往下，滑进抽屉"也是往下。
- 同一对象"信柜"，开（title）往上、收（return）往下，方向语义矛盾。

**改成**

- 信柜改为 `sign={1}`（往下拉），hint 改为"松开，开信柜"不变，视觉上信柜是底部一个抽屉把手，**往下拽 = 把抽屉拉下来打开**。
- 上方向（sign=-1）从此保留给未来的"送回/归还"类动作，不在第一幕使用。

**实现步骤**

1. `TitlePhase.tsx`：信柜 `PullCommit` 的 `sign={-1}` 改为 `sign={1}`。
2. 视觉微调：信柜元素现在在 `mt-auto mb-8`（底部），往下拉会被拉出屏幕下缘——给外层容器加 `pb-2` 或把 threshold 从 42 降到 36，保证拉到底前能过阈值。
3. ~~走查 `ArchivePhase.tsx` / `ReturnPhase.tsx` 里所有和信柜/抽屉相关的文案与方向，统一为"往下"~~（第三幕走查完毕：return 全往下、archive 往上抽开新局，方向无矛盾。此条划掉。）

**验收标准**

- [ ] title 页所有可交互元素只有一个方向：下。
- [ ] 移动端小屏（375×667）下往下拉 36–42px 能稳定触发，不被浏览器下拉刷新/手势冲突（元素已有 `touch-none`，确认即可）。

---

### O1. orbit：情绪块要有各自的表情表现

**现状**

- `OrbitPhase.tsx` 的 `Token`：每个情绪块是一个 `size-11` 的色块 + `ShapeMark`（形状标记：圆/方/菱形/长条的边框小图标）+ 底部标签文字。
- 情绪差异化只在 `EMOTION_MAP`（emotions.ts）的 `color` / `shape` / `label` / `hint` 上；靠近中心的反馈只有透明度变实 + `box-shadow` 光晕，没有"活"感。
- 8 种情绪：gloom 郁闷（方，蓝灰）、wronged 委屈（圆，红）、anxious 焦虑（菱形，橙）、tired 疲惫（长条，绿）、lonely 孤单（圆，紫）、anger 愤怒（方，砖红）、calm 平静（长条，青）、unseen 想被看见（菱形，金）。

**改成**

情绪块内部加一张"脸"，表情由三个状态量驱动：**closeness**（靠近度 0→1）、**held**（被按住）、**gather**（聚拢进度 0→1）。

| 状态 | 表现 |
|---|---|
| 远处（closeness < 0.42） | 沉睡：眼睛是一条线或闭着，整体半透明（现有 0.55+close*0.45 透明度保留） |
| 靠近（closeness ≥ 0.42） | 睁眼，表情浮现，加现有光晕 |
| held（被按住拖动） | 眼睛睁大，瞳孔看向拖动方向（用拖动增量近似） |
| gather（拉「你」时） | 已靠近的：表情安定（嘴角上扬一点/眼睛柔和）；没靠近的：继续沉睡并变淡（现有 `1 - gather * 0.45` 透明度保留） |

每种情绪的表情性格（落在 `EMOTION_MAP` 新增的 `face` 字段里）：

| 情绪 | 眼睛 | 嘴 | 特殊 |
|---|---|---|---|
| gloom 郁闷 | 下垂眼角 | 一字平嘴 | 靠近时眼角更垂 |
| wronged 委屈 | 圆眼，眼眶略红（浅色内圈） | 下撇嘴 | held 时眼眶红更明显 |
| anxious 焦虑 | 快速眨动（CSS 动画 1.2s 一次） | 波浪形小嘴 | 永远静不下来，沉睡时也慢速眨 |
| tired 疲惫 | 半阖眼（上眼睑盖一半） | 微张的小圆嘴 | gather 时眼睑更垂 |
| lonely 孤单 | 眼睛看向斜下方 | 没有嘴 | 全场唯一没有嘴的 |
| anger 愤怒 | 斜眉压眼 | 咬着的平嘴（双线） | held 时眉毛更斜 |
| calm 平静 | 闭眼弯成弧线 | 浅微笑 | 靠近前后变化最小（它本来就平静） |
| unseen 想被看见 | 睁得最大的眼，瞳孔缓慢左右找 | 小圆嘴 | held 时瞳孔立刻锁定手指方向 |

**实现步骤**

1. `emotions.ts` · `Emotion` 类型加字段：
   ```ts
   face: {
     eyes: "closed" | "half" | "downcast" | "round" | "wide" | "side" | "brow";
     mouth: "flat" | "frown" | "wave" | "open" | "none" | "smile" | "bite";
     blink?: boolean;  // anxious 专用
   };
   ```
   按上表给 8 种情绪填 `face`。
2. 新建 `src/game/components/TokenFace.tsx`：SVG 小组件，props 为 `face`、`awake: boolean`（closeness ≥ 0.42）、`held: boolean`、`gaze: {x, y} | null`（视线方向）。尺寸适配 `size-11`（44px）网格内部。
3. `OrbitPhase.tsx` · `Token`：
   - 替换/补充 `ShapeMark`：色块内嵌 `<TokenFace>`；
   - held 时把拖动增量（`useDrag` 的 `movement` 或 `delta`）传给 `gaze`；
   - anxious 的眨动用 CSS `@keyframes` + `transform: scaleY()` 做眼睑，**不用滤镜**。
4. 形状保留（形状是情绪的外轮廓识别，脸是性格）：方形/菱形色块里脸照常画。

**验收标准**

- [ ] 8 种情绪两两可区分：遮住标签文字，只看脸能认出至少 6 种。
- [ ] 拖一个远处情绪靠近中心：能观察到"沉睡 → 睁眼"的变化（阈值 0.42 处切换，可加 0.2s 过渡）。
- [ ] held 时眼睛跟手；松手回到原位表情随之恢复。
- [ ] 拉「你」gather 时，靠近的 2–3 个块表情变化可感知。
- [ ] 移动端 Safari + Chrome 各拖一遍无掉帧（只用 transform/opacity）。

**风险/注意**

- SVG 表情别画太细：44px 里能看清的元素不超过 3 个（两眼一嘴）。
- `token-float`（现有漂浮动画）和眨动动画叠加时注意 `transform` 冲突——漂浮在外层、眨动在内层元素。

---

### O2. orbit → mirror：认领的情绪跟随到下一幕

**现状**

- `commitOrbit()`（store.ts）算出 `fingerprint`（情绪指纹）→ `matchStories(feels, 3, avoid)` 匹配 3 个候选故事 → 切到 mirror。
- `MirrorPhase.tsx` 只渲染 3 张候选卡（城市 · 名字 + opening 开场白），玩家聚拢的情绪块完全消失，"因为你有这些情绪，所以见到这些话"的因果关系不可见。

**改成**

1. **跟随**：进入 mirror 时，已认领的情绪（`ownedOf(fingerprint, 0.42)` 的结果）以缩小形态（28px 小印记，带 O1 的表情）落在 Guide 下方一行，像是"你口袋里带进来的东西"。
2. **因果染色**：每张候选卡角落显示它与你哪个情绪共鸣（小色点 + 情绪名）。实现上需要 `matchStories` 返回每个候选的匹配来源（见实现步骤 2）；若暂不想改 matching 逻辑，备选方案：卡片用你 top-1 情绪的颜色做左边框染色 + 一句"因为{郁闷}，你听见这句"。

**实现步骤**

1. `MirrorPhase.tsx`：Guide 下加一行：
   ```tsx
   const owned = ownedOf(useGame((s) => s.fingerprint), 0.42);
   // 渲染为 28px 小印记横排，复用 O1 的 TokenFace（awake 状态）
   ```
   入场动画：从上方落下的 spring（`spring.settle`），象征"从上一幕带进来"。
2. 因果染色（完整版）：`stories.ts` 的 `matchStories` 目前只返回 3 个 story。改为返回 `{ story, matchedBy: EmotionId }[]`，`store.commitOrbit` 存下 `matchedBy`，`MirrorPhase` 在卡片左上角渲染对应情绪的小印记。
3. 文案配合：Guide body 改为"这些是你带来的。往下拖一句到桌上——认领，不是点选。"

**验收标准**

- [ ] orbit 里认领了几个情绪，mirror 顶部就出现几个小印记，数量、种类一致。
- [ ] 每张候选卡能看出"它和哪个情绪共鸣"。
- [ ] goBack 从 mirror 回 orbit 再进来，印记不丢失（fingerprint 在 store 里，天然保留，确认即可）。

**风险/注意**

- `matchStories` 改动会影响 store 和可能的测试，先看 `stories.ts` 里现有调用方再动手；备选方案（top-1 染色）零侵入，可先上。

---

### O3. orbit 底部状态栏与 Guide 文案去重

**现状**（OrbitPhase.tsx）

- Guide：`title="把靠近你的留下"` `body="拖近中心，就是你。把「你」往下带，才算带走。"`——指令。
- 底部 `<p>`：`held && heldClose >= 0.42` 时显示 `EMOTION_MAP[held].hint`；`owned.length` 时显示"{xx、xx} 靠近了。把「你」往下拉。"；否则显示"先把一块拖近自己"——指令和反馈混在一起，"把「你」往下拉"与 Guide 的"把「你」往下带"重复。

**改成**

- 指令只留 Guide 一处。
- 底部只保留**情境反馈**：
  - held 且已靠近：`EMOTION_MAP[held].hint`（如"胸口像压着一层湿空气"）——情绪的低语，保留；
  - owned.length > 0：只报状态"{郁闷、疲惫} 靠近了"，删掉"把「你」往下拉"；
  - 什么都没有：留空（现有 `min-h-5` 已防布局跳动，直接置空即可）。

**实现步骤**

1. `OrbitPhase.tsx` 底部 `<p>` 的三元表达式改为：
   ```tsx
   {held && heldClose >= 0.42
     ? EMOTION_MAP[held].hint
     : owned.length
       ? `${owned.map((f) => EMOTION_MAP[f.id].label).join("、")} 靠近了`
       : ""}
   ```
2. Guide 文案保持不动（它已是唯一指令源）。

**验收标准**

- [ ] 全幕任意状态下，"下一步该做什么"只有 Guide 在说；底部只说"现在是什么状态"。
- [ ] 三种状态切换时底部高度不跳动。

---

## 第二幕（compose 拼信 → fold 折纸 → throw 投掷）＋ flight 飞行

> 第二幕方向语义确认：第一幕全"往下"（认领、带走），第二幕全"往上"（送纸、折、送窗、投出）——**下 = 收进来，上 = 送出去**。与 T3 的决定一致，后续新交互遵守这套上下对仗。

### C1. mirror → compose：认领的那句话要变成纸

- **现状**：`MirrorPhase.tsx` 没有 `Craft`；认领的卡拖到桌上后 ghost（拖拽幻影）落井消失，180ms 后切 phase，compose 的纸出现时 `layoutId` 是从 orbit 的圆形纸场形变来的（隔了整个 mirror 场景）。认领的话和要写在上面的纸，没有关系。
- **改成**：mirror 的"桌上"井就是下一张 `Craft`（`layoutId="echo-craft"`）。认领的卡落进桌上 → 那张纸形变展开成 compose 的信纸。**话落在哪，信从哪开始写。**
- **实现步骤**：
  1. `MirrorPhase.tsx` 的 well（井，`drag.wellRef` 所在元素）包一层 `Craft`，或在井元素上加 `data-craft` + `layoutId={CRAFT_ID}`（从 continuum.tsx 导入 `CRAFT_ID`）。
  2. `useWellDrag` 的 `landGhost`（幻影落井动画）播完后，井保持"有一张纸在里面"的视觉（垫一层 bg-paper）。
  3. `pickMirror` 切换后，靠 `AnimatePresence mode="sync"` 同时挂载完成形变：井（矮、圆角向上）→ 信纸（高、`rounded-2xl`）。
- **验收标准**：
  - [ ] 拖认领 → 卡落桌 → 桌面纸形变为信纸，全程无元素"凭空出现"。
  - [ ] 不做认领直接返回（goBack）时，orbit 的 Craft 形变链不受影响。
- **状态**：✅ 已接受，待实现

### C2. compose → fold：套用 T1 的形变提交

- **现状**：compose 的 `PullCommit` 为 `sign={-1}`（往上送），提交时纸向上飞出约 135px（52 × 2.6），240ms 后 fold 场景的纸在中央重新出现，视觉跳跃。
- **改成**：`commitBehavior="morph"`（依赖 T1 先实现）——纸随手势上滑一段后直接形变成 fold 桌上的大纸。两场景纸面内容本来就连续（chips 碎片 → 纸上的 chips 文字），只差形变。
- **实现步骤**：
  1. T1 完成后，`ComposePhase.tsx` 的 `PullCommit` 加 `commitBehavior="morph"`。
  2. 确认 `FoldPhase.tsx` 的信纸（`layoutId={CRAFT_ID}`，`folds < 2` 分支）作为形变终点尺寸/位置合适；信纸上的文字（`mirror || letterChips…`）在形变中淡入（0.15s），避免文字突然闪现。
- **验收标准**：
  - [ ] 往上送 → 纸滑向屏幕中上方 → 摊平成 fold 桌上的纸，无飞出、无重现身。
  - [ ] 空信送出（C6 保留此路径）时形变同样成立。
- **状态**：✅ 已接受，待实现（依赖 T1）

### C3. fold 手势纳入统一反馈体系

- **现状**：`FoldPhase.tsx` 没用 `PullCommit`，自带 pointer 手势。进度反馈只有右上角珊瑚圆点（`cornerBg` 随 pull 变深）；"松开，折住"提示只在 v > 0.42 后出现；过阈值无音效/震动（`sfxFold` 是折完之后播的）。
- **改成**：套用 T2 模式——提示文字随 v 渐显；v 过 0.42 时播 `sfxCharge()` + `navigator.vibrate?.(10)`。
- **实现步骤**：
  1. `FoldPhase.tsx` 的 `armed` 提示 `<span>` 改为 MotionValue 驱动透明度（同 T2 的进度提示做法）。
  2. `onMove` 里检测 `armed` 的 false→true 跳变，播 `sfxCharge` + 震动（复用 ref 记录上一次状态，防连播）。
  3. （备选，工作量较大）把对角手势重构为 `PullCommit` 的 `axis="diagonal"` 扩展——先不做，记录在案。
- **验收标准**：
  - [ ] 两刀折叠各有：进度渐显 + 过阈值音效 + 震动一次。
  - [ ] 折完第二刀（folds >= 2）上送飞机的交互反馈不受影响。
- **状态**：✅ 已接受，待实现

### C4. fold → throw：飞机从窗飞入弹弓

- **现状**：folds >= 2 后上送 40px，飞机飞出顶部（`animate(planeY, -240, spring.launch)`），480ms 后 goThrow；throw 场景的飞机出现在**底部弹弓**。窗在上、弹弓在下，空间逻辑断裂。
- **改成**：throw 场景飞机带入场动画：从顶部飞入（`initial: { y: -120, opacity: 0 }` → 弹弓位，`spring.parent`），接住上一幕的飞行方向。
- **实现步骤**：
  1. `ThrowPhase.tsx` 飞机的 `motion.div`（`layoutId={thrown ? undefined : CRAFT_ID}` 那个）加 `initial` / `animate` 入场。
  2. 入场期间（约 0.4s）禁掉 `data-throw-well` 的 `onPointerDown`（用 `thrownRef` 类似的 entering ref），防止玩家在飞机未落位时抓住空气。
- **验收标准**：
  - [ ] fold 上送 → throw 打开 → 飞机从顶部弧线落到弹弓，方向感连续。
  - [ ] 入场期间点击弹弓无响应，落位后才可拉。
- **状态**：✅ 已接受，待实现

### C5. throw → flight：投出的飞机和巡航的飞机是同一架

- **现状**：`fire()` 后飞机淡出到 0.2 并飞向地球瞄准点；`thrown` 后 ThrowPhase 摘掉飞机的 layoutId（`thrown ? undefined : CRAFT_ID`）；FlightPhase 搜索中的巡航飞机没有 layoutId，是 `initial opacity 0.5 scale 0.72` 的新飞机。形变链被亲手剪断。
- **改成**：巡航飞机挂 `layoutId="echo-craft"`；投出的飞机不淡出，形变到巡航起点。**你投出去的那架 = 夜里找人的那架——它是你的信，不能换。**
- **注意**：与 F1（雷电躲避）合并实施——F1 本来就要重写 FlightPhase 的飞机渲染，C5 的形变在 F1 的新结构里一并落地，避免改两遍。
- **验收标准**：
  - [ ] 松手 → 飞机飞向地球 → 形变进入夜空巡航，全程同一架。
  - [ ] found（找到人）后飞机形变为可下拉的 PullCommit 飞机（现有行为），链路不断。
- **状态**：✅ 已接受，待实现（与 F1 合并实施）

### C6. 空信可送出，但"捡来的句子"要有视觉区别

- **决策**：保留空信路径（"没话可说的夜晚也值得寄出"），fold 纸上 fallback 显示 mirror 句子时给视觉区别。
- **实现**：`FoldPhase.tsx` 纸上文字区域：`mirror || letterChips…`——当 `letterChips.length === 0 && !extraLine` 时，mirror 句子用斜体 + `text-ink/45`（比正常淡），并可加前缀"你捡到的："。compose 的 Guide 文案"也可以直接把纸往上送"保留。
- **验收标准**：
  - [ ] 空信送出时，fold 纸上的句子一眼能看出"这不是我写的"。
  - [ ] 有内容的信不受影响。
- **状态**：✅ 已接受（方案一），待实现

### F1. flight：穿过雷雨去抵达（新增玩法 · 本次讨论提出）

**设计意图**

- 现状的 FlightPhase 是被动等待：`launch()` 后 `runMatch`（agent 匹配）最长 22s（`Promise.race` 超时），玩家只能看进度条爬。
- 改成：**agent 搜集信息（情绪环指纹 + 信的内容）并开始匹配的时间 = 玩家驾驶纸飞机穿过雷雨的时间**。等待不再是空洞，而是叙事本身。
- 心理学隐喻：雷电是"说出没说出口的话"之前要穿越的恐惧与预演（呼应 anxious 的 mirror"我在预演还没发生的失败"）；躲开它们、坚持飞完 = 为抵达交流状态花费的功夫——点题"说出自己想说但没说出的话"。

**玩法规格**

- **操控**：手指按住飞机拖动（上下左右全向），飞机以弹簧滞后跟随（`spring.parent`：stiffness 160 / damping 18，略有甩尾的"纸感"）。手指离开则飞机保持当前位置缓飘。
- **雷电**：
  - 云层中随机生成雷云，落雷前 **0.6s 预警**（云内闪烁发白光），然后一道竖直闪电劈下。
  - 闪电命中判定：飞机中心与闪电路径的水平距离 < 28px 且在闪电竖直范围内。
- **命中后果（无失败态）**：这个游戏没有 game over。被劈中 = 颠簸：
  - 屏幕震动一下（`punch` MotionValue 已有此机制可复用）+ 音效 `sfxThunder`（新增）+ `vibrate([20, 40, 20])`；
  - 飞机短暂失控 0.4s（跟随弹簧暂时断开，自旋半圈）；
  - 进度条回退 3–5%（"被打乱了节奏"，不惩罚时间——agent 匹配本来就要那么久）；
  - 视觉累积：被劈 1 次机翼焦一角，2 次焦两角（cosmetic，纯外观，带进 encounter 场景更有故事感；**回程飞机（R2）也带同样的焦角——对方收到的是你那架，回来也是那架**）。
- **难度**：与 `throwPower`（投掷力度）挂钩——投得越满，巡航越快（现有 `cruise` 公式保留），雷云密度越高。蓄力投掷的承诺感延续到难度上。
- **结束条件**：`runMatch` 返回（或超时 fallback）→ 雷雨散场：云层向两侧分开，飞机切到现有"往下拉，落到桌上"的 `PullCommit`。**不要**再加窗光圆环 / 「穿过来了」的穿梭到场（见 F2）。
- **时长地板**：即使 agent 秒回（缓存命中），飞行也至少持续 ~5s；太短则云层直接散开。

**文案配合**

- Guide 搜索中：`"这不是检索。是你先穿过自己的夜。"`（替换现文案，点出隐喻）
- 被劈中瞬间底部小字：`"差点就掉头了"` / `"还飞着"`（随机，克制，不说教）
- 到达时：用 store 的 `searchNote`——成功 `"到了 {echo.city}，{echo.name} 读完了你的信"`，失败沿用线路不稳兜底。不要写「穿过来了」。

**实现步骤**

1. `FlightPhase.tsx` 重写中部区域：
   - 飞机改为可拖拽跟随（复用 PullCommit 的 pointer 监听模式，但不设阈值提交——纯跟随；found 后才切换为现有下拉 PullCommit）；
   - 雷云：基于现有两个模糊云 div 升级为 3–5 个障碍云（缓慢平移，复用现有 `easeInOut` 往返动画模式）；
   - 闪电：SVG 竖直折线，预警闪烁（opacity 脉冲 0.6s）→ 劈下（0.12s 显形）→ 消散；生成节奏用 `setInterval` + 随机，频率随 `throwPower` 缩放；
   - 碰撞检测：rAF（requestAnimationFrame，逐帧回调）循环里比对飞机 MotionValue 坐标与各闪电路径，命中触发上述颠簸逻辑。
2. `audio.ts` 新增 `sfxThunder()`：低频噪声爆发——`sawtooth` 60→30Hz 下滑 0.5s + 滤波器低通扫频（可参考 `sfxWhoosh` 的滤波器写法），增益 0.09。
3. 失控表现：命中后 0.4s 内飞机 rotate 自旋（`animate(rot, rot + 180, …)`），跟随弹簧的 stiffness 临时降到 40 再恢复。
4. reduced-motion（减弱动效）：无雷电，飞机自动平缓巡航（退化为现状的巡航动画）。
5. C5 的 layoutId 形变在新结构里一并实现（巡航飞机挂 `layoutId="echo-craft"`）。
6. 焦角视觉：`Plane` 组件加 `scorched?: 0 | 1 | 2` prop，机翼尖端叠暗色小三角。

**验收标准**

- [ ] agent 匹配期间玩家有事可做：全程可操控飞机，雷电密度适中（10s 内约 3–6 道预警闪电）。
- [ ] 被劈中有明确反馈（震动/音效/颠簸/进度回退），但不会失败、不会重来。
- [ ] agent 返回时雷雨散场、进入下拉落地，衔接自然；秒回时至少飞 5s。到达时无窗光门户、无「穿过来了」。
- [ ] 焦角状态带进 encounter（相遇）场景可见。
- [ ] reduced-motion 下退化为无雷电巡航。
- [ ] 22s 超时 fallback（信柜取旧信）路径下，雷雨同样散场，文案改为"线路不稳，改从信柜里取一封相近的旧信"（现有 searchNote 逻辑保留）。

**状态**：已落地后又按 F3 整段撤销（跟飞 / 躲障不再要）

### F2. flight：去掉到达穿梭表现

**现状**

- `FlightPhase.tsx` 在 `ready`（找到人且过 5s 地板）时：
  - Guide 标题写成 `"穿过来了。{echo.city} 在下面。"`，盖住 store 已写好的 `searchNote`（`"到了 {城}，{名} 读完了你的信"`）；
  - 飞机下方再叠一个 `h-28 w-28` 的太阳色径向光斑，像从门户穿过来。
- 玩家看见的是「穿梭到场」，不是「人找到了，把飞机拉到桌上」。

**改成**

1. 删掉 ready 态的窗光圆环，不再演穿梭。
2. Guide 标题直接用 `searchNote`；成功/超时两条文案都走 store，飞行层不再另写「穿过来了」。
3. 正文只留落地指令：`"把飞机往下拉，落到桌上。"`
4. 雷雨散场、下拉 `PullCommit`、最短 5s 都保留。

**实现步骤**

1. `FlightPhase.tsx`：删 `ready` 分支里 `top-[64%]` 的径向光斑。
2. `FlightPhase.tsx`：`title` 改为 `note || (echo ? \`到了 ${echo.city}\` : "到了")`，`body` 改为落地指令。
3. 本文 F1 结束条件 / 到达文案与 `docs/game-logic-preview.md` §3.5 对齐。

**验收标准**

- [x] 找到人后标题是「到了 {城}，{名} 读完了你的信」或线路不稳兜底，页面上没有「穿过来了」。
- [x] 到达画面没有窗光圆环。
- [x] 仍可下拉过 48 落地，进 encounter。

**状态**：✅ 已完成（`e97d25d`）；跟飞/躲障由 F3 一并拿掉

### F3. flight：去掉跟飞和躲障，回到等待巡航

**对齐（先结构，再编码）**

`flight` 只做一件事：等 `runMatch`，人到了再下拉落地。寻找中手不能开飞机，也不能躲东西。

```
throw.launch
    → flight.searching     看：飞机自己巡航 + 进度条
                           手：无
                           后台：runMatch（≤22s）
    → flight.found         看：searchNote
                           手：下拉 PullCommit 48 → arrive() → encounter
```

| 层 | 留 | 删 |
|---|---|---|
| store | `searching` / `searchNote` / `arrive` / `throwPower`（只影响巡航快慢） | 飞行不再调用 `markScorch` |
| 手 | 找到后往下拉 | pointer 跟随、`__flightTest`、拾灯 |
| 障碍 | — | 雷云 warn/boom、闪白、碰撞、击中文案、`sfxThunder` |
| 看 | 两朵景云慢漂、自动巡航、进度条、`layoutId` 同一架 | 残影、镜头追随、5s 地板、窗光圆环、「穿过来了」 |
| 文案 | 寻找中用 `searchNote`；找到后落地指令 | 「手可以带着飞」「飞机跟着你」「绕开」 |

**改成**

`FlightPhase.tsx` 回到初版巡航骨架（自动飞 + 进度 + 下拉），接上现有 `searchNote` 和 `commitBehavior="morph"`。

**验收标准**

- [ ] 寻找中挪鼠标，飞机不跟手；页面无「跟着你 / 绕开 / 手可以带着飞」。
- [ ] 没有雷击、焦角增长、灯被吃掉。
- [ ] 找到后标题是 `searchNote`，下拉过 48 进 encounter。
- [ ] `window.__flightTest` 不存在。

**状态**：✅ 已接受，本提交落地

---

## 第三幕（encounter 相遇 → return 回信 → archive 信柜）

> 方向语法闭环确认：第三幕全"往下"（拆回信、收进抽屉），archive 的"往上抽"开启新一轮——**下 = 收进来，上 = 送出去**的循环成立。T3 实现步骤第 3 条已划掉。
> 顺的设计记录：return 的双阈值下拉（28px 拆开 / 108px 进抽屉，一根手势两层承诺）是全游戏最佳交互，值得推广；encounter 的"先听"门槛（bloom 需收到对方的话 + 700ms 才出选项）用节奏强制倾听，保留。

### R1. flight → encounter：飞机落下来摊开成桌面

- **现状**：flight 的落地 PullCommit 是 fly 模式（飞出后切场景）；encounter 的对话桌面虽是 Craft，但形变源头（飞机）已飞走。
- **改成**：落地 PullCommit 用 `commitBehavior="morph"`（T1 机制，第三次复用）——飞机降下来，在桌上摊开成那叠对话的纸。
- **实现步骤**：
  1. `FlightPhase.tsx` 的落地 PullCommit 加 `commitBehavior="morph"`（依赖 T1；若与 F1 合并实施，在 F1 重写里一并做）。
  2. 形变终点：`EncounterPhase.tsx` 的桌面 Craft（`bg-paper/35 rounded-t-2xl`）。
- **验收标准**：
  - [ ] 往下拉 → 飞机落向屏幕中下 → 摊开成桌面，无飞出、无重现身。
- **状态**：✅ 已接受，待实现（依赖 T1，可与 F1 同 PR）

### R2. encounter → return：回信要"飞"回来，带着焦角

- **现状**：第 3 轮回复后 `runSeal`，等待文案说"纸正在折回来"，但视觉上什么都没有，场景直接切到 return 的未拆信。去程有穿越雷雨的旅途（F1），回程没有。
- **改成**：return 场景入场：一架飞机从顶部飞入（约 0.6s），在屏幕中央收翅、形变成"未拆的回声"。**若 F1 已实现焦角，回程飞机带寄出时的焦角。**
- **实现步骤**：
  1. `ReturnPhase.tsx`：Craft 内加入场动画——`initial: { y: -140, opacity: 0, scale: 0.7 }` → 信位（`spring.parent`），或者用一架小飞机元素飞入后 crossfade 成信（二选一，真机看哪个更顺）。
  2. 焦角：`scorched` 状态需存进 store（如 `hits` 或新字段 `scorch: 0|1|2`），return 入场飞机和 encounter 飞机都读它。
  3. 等待期（"纸正在折回来"）不打断：入场动画只在 phase 切到 return 后播。
- **验收标准**：
  - [ ] 第 3 轮回复 → 等待 → 飞机从顶部飞入收翅成信，回程有旅途感。
  - [ ] F1 被劈过的焦角出现在回程飞机上。
- **状态**：✅ 已接受，待实现

### R3. return 双阈值手势纳入反馈体系＋抽象多阈值 PullCommit

- **现状**：return 是第三个"私搭手势"（fold 是第二个）。拉动中无进度提示；过 28px / 108px 两阈值无音效/震动标记（`sfxPaper` 松手后才播）；抽屉只有 `filing` 珊瑚色高亮。
- **改成**：
  - 过 28px（拆开）：`sfxPaper()`（从松手后挪到阈值瞬间）+ `vibrate(8)`；
  - 过 108px（进抽屉）：`sfxCharge()` + `vibrate(16)`；
  - 拉动中提示文字随进度显影（同 T2）。
- **抽象（值得做）**：把"多阈值下拉"抽象成 `PullCommit` 的 `stops: number[]` 模式（如 `stops={[28, 108]}`），每个 stop 有自己的 hint/音效/回调。fold 的对角手势未来也可并入（对应 C3 备选）。return 场景是第一个用户。
- **验收标准**：
  - [ ] 两道阈值各有明确的"咔哒"感（音效+震动+文案切换）。
  - [ ] 双阈值行为不回归：拉一半松手停住（拆开态），拉满才进抽屉。
- **状态**：✅ 已接受，待实现

### R4. return → archive：信要落进抽屉，不是飞过抽屉

- **现状**：提交时信 `animate(y, 220, spring.launch)` 向下飞 220px，穿过底部的抽屉，不是落进去。`useWellDrag` 里已有现成的 `landGhost`（落井动画）可借鉴。
- **改成**：信的收尾动画向抽屉中心收拢（scale 至 0.7 + 位移到抽屉中心 + 淡入抽屉内部）；抽屉做"吞下去"的回弹（`spring.tick`）。
- **实现步骤**：
  1. `ReturnPhase.tsx` 的提交分支：计算抽屉中心坐标（给抽屉加 ref），`animate(y, 抽屉中心距离)` + `animate(scale 1→0.7)` + 透明度末端淡出。
  2. 抽屉元素在吞入瞬间播 `spring.tick` 缩放脉冲。
- **验收标准**：
  - [ ] 信视觉上进抽屉，抽屉有吞咽回弹；随后 saveReturn → archive 形变链（return Craft → archive 阅读页 Craft，已存在）不受影响。
- **状态**：✅ 已接受，待实现

### R5. archive → 新一轮：空白纸接入形变链，循环闭环

- **现状**：archive 底部"空白的一张"是普通 div（PullCommit 里无 Craft）；"往上抽"后 `startNew` 进 orbit，orbit 的圆形纸场从上上个场景的 Craft 形变过来。循环"纸→飞机→信→纸"的最后一棒没接进形变链。
- **改成**：空白纸卡片包 `Craft`。往上抽 = 抽出新纸，形变成 orbit 的圆形纸场——**T1"卡片摊开成房间"的循环版**：第一局的房间从标题卡摊开，之后每局从信柜抽出的纸摊开。
- **实现步骤**：
  1. `ArchivePhase.tsx` 的 PullCommit 子元素外包 `<Craft>`。
  2. PullCommit 用 `commitBehavior="morph"`（依赖 T1）。
- **验收标准**：
  - [ ] 往上抽 → 空白纸摊大成 orbit 纸场，循环无接缝。
  - [ ] 从 title 进 archive 再抽纸，形变起点正确（archive 的阅读 Craft 与空白纸 Craft 不冲突——注意同屏只能有一个 `layoutId="echo-craft"`，阅读态与空白卡同时存在时需给空白卡 `layout={false}` 或在非阅读态才挂 layoutId）。
- **状态**：✅ 已接受，待实现（依赖 T1）

---

## 实现顺序建议

**第一幕**：T2 + T3 → O3 → T1 → O1 → O2（每项单独 commit，O1/O2 可合一个 PR）

**第二幕**：C6（纯展示层，最快）→ C3（fold 反馈）→ C1（mirror 出口形变）→ C2（依赖 T1）→ C4（throw 入场）→ C5（同一架飞机进飞行）。F1 跟飞躲障已按 F3 撤销。

**第三幕**：R4（纯动画收尾，独立）→ R2（回程飞机入场）→ R3（双阈值反馈 + stops 抽象）→ R1 / R5（都依赖 T1，最后补形变链）

**全局依赖**：T1（morph 提交）是 C2 / R1 / R5 的共同前置；F1 是 R2 焦角的前置。建议 T1 在第一幕内尽早做。

---

## 待讨论

- F1 雷电密度/难度曲线真机调试后再定稿参数
- T1 形变手感真机验证后再定稿参数（spring.settle 是否够"纸感"）
- R2 入场动画二选一（飞机收翅 crossfade 成信 vs 直接形变）真机对比后定稿
- 全部实现完后做一次三幕连跑的连贯性回归（一镜到底检查：手里是否永远有东西）
